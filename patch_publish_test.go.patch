--- internal/pipe/gentoo/publish_test.go
+++ internal/pipe/gentoo/publish_test.go
@@ -330,8 +330,8 @@
	require.NoError(t, err)
	content, err := os.ReadFile(filepath.Join(clone, "published"))
	require.NoError(t, err)
-	// Allow either \n or \r\n to support git clone on Windows checking out with crlf
-	require.True(t, string(content) == "yes\n" || string(content) == "yes\r\n", "content should be yes\\n or yes\\r\\n, got %q", string(content))
+	// Normalize line endings to support git clone on Windows checking out with crlf
+	require.Equal(t, "yes\n", strings.ReplaceAll(string(content), "\r\n", "\n"))
 }

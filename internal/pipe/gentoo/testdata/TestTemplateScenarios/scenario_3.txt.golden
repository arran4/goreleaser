# This file was generated by GoReleaser. DO NOT EDIT.
EAPI=8

DESCRIPTION=""
HOMEPAGE=""
SRC_URI="
"

LICENSE=""
SLOT="0"
KEYWORDS=""
IUSE=""

S="${WORKDIR}"

src_install() {
  if use amd64; then
    doexe "prog1_x86" || die "Failed to install binary"
  fi
  doexe "prog2" || die "Failed to install binary"
  if use arm64; then
    doexe "prog1_arm" || die "Failed to install binary"
  fi
  if use arm64; then
    doexe "prog3" || die "Failed to install binary"
  fi
}